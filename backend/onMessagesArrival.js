import firebase         from './firebase';

export default onMessagesArrival = (callback) => {
  /*
  db.collection('messages').where("state", "==", "CA")
  .onSnapshot((querySnapshot) => {
    var cities = [];
    querySnapshot.forEach((doc) => {
      cities.push(doc.data().name);
    });
  });
  */
}
